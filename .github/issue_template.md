<!-- If this issue reports package use, a question, a bug, or a similar topic, add your query. 
     Add your devtools::session_info() or sessionInfo() in the code block below. 
     If ckanr throws an error, also add the traceback() of the error. If not, delete this text and continue. 
-->

<details>
<summary><strong>Session Info and Traceback</strong></summary>

<!-- SessionInfo shows the exact circumstances of the error: OS, installed packages. -->
```r
# devtools::session_info()  # or
utils::sessionInfo()

```

<!-- If ckanr throws an error, run traceback() immediately after the error and paste the results here. -->
```r
traceback()

```
</details>
