% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{files}
\alias{files}
\alias{file_create}
\alias{file_register}
\alias{file_show}
\alias{file_delete}
\alias{file_rename}
\alias{file_pin}
\alias{file_unpin}
\alias{file_ownership_transfer}
\alias{file_owner_scan}
\title{File management}
\usage{
file_create(
  upload,
  name = NULL,
  storage = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_register(
  location = NULL,
  storage = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_show(
  id,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_delete(
  id,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_rename(
  id,
  name,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_pin(
  id,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_unpin(
  id,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_ownership_transfer(
  id,
  owner_id,
  owner_type,
  force = FALSE,
  pin = FALSE,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

file_owner_scan(
  owner_id,
  owner_type,
  start = NULL,
  rows = NULL,
  sort = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{upload}{(character) Local path of the file to upload for
\code{file_create()}. Required.}

\item{name}{(character) Human-readable file name. For \code{file_create()},
defaults to the uploaded file's name when \code{upload} is a local path.}

\item{storage}{(character) Name of the storage handling the upload.
Defaults to the configured \code{default} storage.}

\item{url}{Base URL to use. Default: https://demo.ckan.org/. See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key. Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
the table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible. The result
can vary from a vector, list or data.frame. (required)}

\item{...}{Extra curl arguments. The function passes them to \code{\link[crul]{verb-POST}} (optional)}

\item{location}{(character) Location of the file in the storage for
\code{file_register()}. Required.}

\item{id}{(character or \code{ckan_file}) File identifier.}

\item{owner_id}{(character) ID of the new owner for
\code{file_ownership_transfer()} and \code{file_owner_scan()}.}

\item{owner_type}{(character) Type of the new owner (for example
\code{"package"}, \code{"user"}, \code{"group"}, \code{"organization"}).}

\item{force}{(logical) For \code{file_ownership_transfer()}, move the file
even if it is pinned. Default: \code{FALSE}.}

\item{pin}{(logical) For \code{file_ownership_transfer()}, pin the file after
transfer to stop future transfers. Default: \code{FALSE}.}

\item{start}{(numeric) Index of the first row for \code{file_owner_scan()}.}

\item{rows}{(numeric) Number of rows to return for \code{file_owner_scan()}.}

\item{sort}{(character) \code{File} column used for sorting in
\code{file_owner_scan()}. Optional; when \code{NULL} the server default
(\code{"name"}) applies.}
}
\description{
These helpers wrap CKAN 2.12 \verb{file_*} endpoints. Files are first-class
entities in CKAN 2.12 (see
\url{https://docs.ckan.org/en/2.12/api/#ckan-logic-action-file} and
\url{https://github.com/ckan/ckan/pull/9026}). The target CKAN instance must
run CKAN 2.12 or later; each wrapper calls \code{ensure_action_available()}
so it fails clearly on older instances.
}
\details{
By default only sysadmins may call \code{file_create()}; see
\code{ckan.files.authenticated_uploads.allow} in the CKAN docs for granting
access to registered users.
}
\examples{
\dontrun{
ckanr_setup(url = "https://demo.ckan.org/", key = getOption("ckan_demo_key"))
f <- file_create(upload = "path/to/file.csv")
file_show(f$id)
}
}
