% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admin_ops.R
\name{api_tokens}
\alias{api_tokens}
\alias{api_token_list}
\alias{api_token_create}
\alias{api_token_revoke}
\title{API token helpers}
\usage{
api_token_list(
  user_id = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

api_token_create(
  user,
  name,
  extra_fields = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

api_token_revoke(
  token = NULL,
  jti = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{user_id}{(character) User identifier to filter tokens.}

\item{url}{Base url to use. Default: https://demo.ckan.org/ See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key, Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible, so the result
can vary from a vector, list or data.frame. (required)}

\item{...}{Curl args passed on to \code{\link[crul]{verb-POST}} (optional)}

\item{user}{(character) Name or id of the token owner.}

\item{name}{(character) Friendly token name.}

\item{extra_fields}{(list) Optional named list of additional fields accepted
by CKAN or extensions.}

\item{token}{(character) Encoded API token value.}

\item{jti}{(character) Token identifier (overrides \code{token} when provided).}
}
\description{
Inspect, create, and revoke API tokens for a user.
}
\examples{
\dontrun{
ckanr_setup(url = "https://demo.ckan.org/", key = "my-ckan-key")
api_token_list(user_id = "my-user-id")
}
}
