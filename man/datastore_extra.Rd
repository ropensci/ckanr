% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datastore_extra.R
\name{datastore_extra}
\alias{datastore_extra}
\alias{ds_upsert}
\alias{ds_delete}
\alias{ds_records_delete}
\alias{ds_info}
\alias{ds_function_create}
\alias{ds_function_delete}
\alias{ds_run_triggers}
\title{Datastore write helpers (CKAN 2.12+ parameters from the start)}
\usage{
ds_upsert(
  resource_id,
  records = NULL,
  method = "upsert",
  force = FALSE,
  include_records = FALSE,
  calculate_record_count = "background",
  dry_run = FALSE,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_delete(
  resource_id = NULL,
  filters = NULL,
  force = FALSE,
  include_deleted_records = FALSE,
  calculate_record_count = "background",
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_records_delete(
  resource_id,
  filters,
  force = FALSE,
  include_deleted_records = FALSE,
  calculate_record_count = "background",
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_info(
  resource_id,
  include_meta = TRUE,
  include_fields_schema = TRUE,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_function_create(
  name,
  rettype = "trigger",
  definition,
  or_replace = FALSE,
  argmode = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_function_delete(
  name,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

ds_run_triggers(
  resource_id,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{resource_id}{(character) Resource id that the data is stored against.}

\item{records}{(list|data.frame) The data, e.g.
\code{list(list(a = 1, b = "xyz"))}.}

\item{method}{(character) Upsert method: \code{"upsert"} (default),
\code{"insert"}, or \code{"update"}. \code{upsert}/\code{update} require a unique key or
\verb{_id} field.}

\item{force}{(logical) Edit a read-only table. Default: \code{FALSE}.}

\item{include_records}{(logical) If \code{TRUE}, return the actual affected
rows in the response (CKAN 2.12+). Default: \code{FALSE}.}

\item{calculate_record_count}{(logical|character) \code{FALSE} skips the
count update, \code{TRUE} updates immediately, \code{"background"} schedules a
background job (default).}

\item{dry_run}{(logical) Abort the transaction instead of committing,
e.g. to check validation errors. Default: \code{FALSE}.}

\item{url}{Base URL to use. Default: https://demo.ckan.org/. See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key. Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
the table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible. The result
can vary from a vector, list or data.frame. (required)}

\item{...}{Extra curl arguments. The function passes them to \code{\link[crul]{verb-POST}} (optional)}

\item{filters}{(list|character) Matching conditions to select, e.g.
\code{list(name = "fred")}. CKAN 2.12+ accepts advanced syntax (ranges, nested
AND/OR); the argument passes through untouched.}

\item{include_deleted_records}{(logical) If \code{TRUE}, return the full
values of deleted records (CKAN 2.12+). Default: \code{FALSE}.}

\item{include_meta}{(logical) For \code{ds_info()}, return table size, index
size, row count and aliases (CKAN 2.12+). Default: \code{TRUE}.}

\item{include_fields_schema}{(logical) For \code{ds_info()}, return per-field
index/unique/notnull status (CKAN 2.12+). Default: \code{TRUE}.}

\item{name}{(character) Trigger function name for
\code{ds_function_create()} / \code{ds_function_delete()}.}

\item{rettype}{(character) Set to \code{"trigger"} (only trigger functions
may be created at this time).}

\item{definition}{(character) PL/pgSQL function body for the trigger.}

\item{or_replace}{(logical) Replace the function if it exists.
Default: \code{FALSE}.}

\item{argmode}{(character) Argument mode for custom DataStore SQL
function parameters, e.g. \code{"in"}, \code{"inout"}, \code{"out"} (CKAN 2.12+,
\url{https://github.com/ckan/ckan/pull/8279}). Optional.}
}
\description{
Wrappers for \code{datastore_upsert}, \code{datastore_delete},
\code{datastore_records_delete}, \code{datastore_info}, \code{datastore_function_create},
\code{datastore_function_delete}, and \code{datastore_run_triggers}. \code{ds_search()}
and \code{ds_create()} cover read/create; these helpers complete the
write/info coverage with the CKAN 2.12 parameters (\code{include_records},
\code{include_meta}, \code{include_fields_schema}, \code{argmode}) available from the
start. See
\url{https://docs.ckan.org/en/2.12/maintaining/datastore.html#the-data-api}.
}
\examples{
\dontrun{
ckanr_setup(url = "https://demo.ckan.org/", key = getOption("ckan_demo_key"))
ds_info(resource_id = "my-resource-id")
ds_upsert(resource_id = "my-resource-id",
  records = list(list(a = 1, b = "xyz")), method = "upsert")
}
}
