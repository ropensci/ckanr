% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/membership.R
\name{member_create}
\alias{member_create}
\title{Create or update a membership via member_* endpoints}
\usage{
member_create(
  id,
  object,
  object_type,
  capacity,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{id}{(character, \code{ckan_group}, or \code{ckan_organization}) Identifier for the container.}

\item{object}{(character) The object to add (dataset id, user name, etc.).}

\item{object_type}{(character) Type of object you add (\code{"user"}, \code{"package"}, ...).}

\item{capacity}{(character) Capacity for the membership.}

\item{url}{Base URL to use. Default: https://demo.ckan.org/. See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key. Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
the table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible. The result
can vary from a vector, list or data.frame. (required)}

\item{...}{Extra curl arguments. The function passes them to \code{\link[crul]{verb-POST}} (optional)}
}
\description{
Create or update a membership via member_* endpoints
}
