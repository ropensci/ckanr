% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_package_show.R
\name{group_package_show}
\alias{group_package_show}
\title{Show the datasets of a group.}
\usage{
group_package_show(
  id,
  limit = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{id}{(character or \code{ckan_group}) The id or name of the group, or a
\code{ckan_group} object.}

\item{limit}{(numeric) The maximum number of datasets to return (optional).}

\item{url}{Base URL to use. Default: https://demo.ckan.org/. See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key. Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
the table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible. The result
can vary from a vector, list or data.frame. (required)}

\item{...}{Extra curl arguments. The function passes them to \code{\link[crul]{verb-POST}} (optional)}
}
\value{
A list of \code{ckan_package} objects. With \code{as = "table"}, a data.frame;
with \code{as = "json"}, the raw JSON response.
}
\description{
Return the datasets (packages) that belong to a group. See
\url{https://docs.ckan.org/en/2.11/api/#ckan.logic.action.get.group_package_show}
for the official API contract.
}
\examples{
\dontrun{
ckanr_setup(url = "https://demo.ckan.org/", key = getOption("ckan_demo_key"))

res <- group_list()
group_package_show(res[[1]]$name)
group_package_show(res[[1]], limit = 5, as = "table")
}
}
\references{
https://docs.ckan.org/en/2.11/api/#ckan.logic.action.get.group_package_show
}
