% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activity.R
\name{activity_purge}
\alias{activity_purge}
\alias{activity_delete}
\alias{activity_delete_all}
\alias{activity_delete_counts}
\title{Activity purge helpers (CKAN 2.12+)}
\usage{
activity_delete(
  id = NULL,
  start_date = NULL,
  end_date = NULL,
  offset_days = NULL,
  keep = NULL,
  batch_size = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

activity_delete_all(
  batch_size = NULL,
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)

activity_delete_counts(
  url = get_default_url(),
  key = get_default_key(),
  as = "list",
  ...
)
}
\arguments{
\item{id}{(character) Activity identifier to delete via
\code{activity_delete()}. Alternatively provide \code{start_date} + \code{end_date}
or \code{offset_days}.}

\item{start_date}{(character) Start of the deletion range (ISO 8601).}

\item{end_date}{(character) End of the deletion range (ISO 8601).}

\item{offset_days}{(numeric) Delete activities older than this many days.}

\item{keep}{(numeric) Optional. Keep this many most recent activities per
item; delete only older ones in the range.}

\item{batch_size}{(numeric) Optional batch size for large tables.}

\item{url}{Base URL to use. Default: https://demo.ckan.org/. See
also \code{\link{ckanr_setup}} and \code{\link{get_default_url}}.}

\item{key}{A privileged CKAN API key. Default: your key set with
\code{\link{ckanr_setup}}}

\item{as}{(character) One of list (default), table, or json. Parsing with
the table option uses \code{jsonlite::fromJSON(..., simplifyDataFrame = TRUE)},
which attempts to parse data to data.frame's when possible. The result
can vary from a vector, list or data.frame. (required)}

\item{...}{Extra curl arguments. The function passes them to \code{\link[crul]{verb-POST}} (optional)}
}
\description{
These helpers wrap the CKAN 2.12 activity purge feature
(\url{https://github.com/ckan/ckan/pull/8189}). You must have sysadmin
rights and the target instance must enable the \code{activity} plugin with
CKAN 2.12 or later. Each wrapper calls \code{ensure_action_available()} so
it fails clearly on older instances.
}
\examples{
\dontrun{
ckanr_setup(url = "https://demo.ckan.org/", key = getOption("ckan_demo_key"))
activity_delete_counts()
activity_delete(offset_days = 365)
}
}
